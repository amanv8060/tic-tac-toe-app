import 'package:flutter/material.dart';

class GameEndDialog extends StatelessWidget {
  final int result;
  const GameEndDialog({Key? key, required this.result}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
